name: bug_tracker_frontend
description: Bug tracker UI

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  google_fonts: ^6.1.0
  shared_preferences: ^2.2.3
  file_picker: ^8.1.2

flutter:
  uses-material-design: true